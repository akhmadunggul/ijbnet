import NotificationsPage from '../../components/NotificationsPage';

export default function CandidateNotifications() {
  return <NotificationsPage extraInvalidations={[['notifications-preview']]} />;
}
